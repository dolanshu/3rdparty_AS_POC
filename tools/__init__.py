"""Standalone utility tools for the 3rd-party AS POC.

The tools package is **not** an AS module and must not import ``as_app``,
``anti_fraud_as`` or ``as_platform``. Each tool talks SIP or HTTP to the
running AS processes.
"""
