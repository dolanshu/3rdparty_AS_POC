# Acceptance criteria

One row per acceptance item: criterion, verification command, expected result, related
requirement. An item is accepted only with evidence per `AGENT.md` section 4.8; the
evidence is recorded in `docs/acceptance/report.md`.

Legend: **accepted** — executed with evidence · **open** — not executed yet ·
**planned** — belongs to a later milestone.

## M0 — Foundation

| ID | Criterion | Verification command | Expected result | Requirement |
| --- | --- | --- | --- | --- |
| ACC-M0-001 | Repository skeleton and meta files exist exactly as `AGENT.md` section 5 | `uv run pytest tests/unit/test_repository_baseline.py -q` | all checks pass (directories, meta files, no forbidden module names, licence header in every source file) | REQ-NF-007 |
| ACC-M0-002 | `uv` installs the locked environment and sippy 2.4.2 runs a minimal stack on Python 3.10 | `uv sync --frozen && uv run python tools/sippy_probe.py` | `sippy : 2.4.2`, a `404` response to the probe INVITE, last line `minimal SipTransactionManager + ED2.loop() stack: OK` | REQ-NF-003, REQ-NF-008 |
| ACC-M0-003 | Documentation baseline per `AGENT.md` section 4.2 is present | `uv run pytest tests/unit/test_repository_baseline.py -q -k documentation` | all checks pass (SRS, HLD, LLD, operations, acceptance, demo script, glossary, production gaps) | REQ-NF-007 |
| ACC-M0-004 | Sample routing data meets `AGENT.md` section 4.6 | `uv run python tools/show_rules.py` | 10–20 rules with priorities, ranges across several operators plus special service numbers, at least two next hops with priority and failover, all four number formats exercised | REQ-F-003, REQ-F-004, REQ-F-005 |
| ACC-M0-005 | Configuration knobs of `AGENT.md` section 8 are declared and the startup self-check passes | `uv run python -m as_app.main --self-check-only; echo $?` | exit code `0` and a log line with `event: startup self-check passed` | REQ-F-013, REQ-F-014 |
| ACC-M0-006 | A changed rules file is picked up; an invalid one keeps the previous rule set | `uv run pytest tests/integration -q -k reloaded` | the test passes: reload activates the new file, and a broken edit leaves the previous rule set active | REQ-F-005 |
| ACC-M0-007 | Structured logging with the mandatory field set and the `AS-*` error model | `uv run pytest tests/unit/test_observability.py tests/unit/test_errors.py -q` | all tests pass; every log line carries `timestamp`, `level`, `module`, `call_id`, `direction`, `peer`, `event` | REQ-F-010, REQ-F-015, REQ-NF-005 |
| ACC-M0-008 | ADR-0001 … ADR-0006 exist with context, decision and consequences | `ls docs/architecture/adr/` | six files, `0001` … `0006` | REQ-NF-007 |
| ACC-M0-009 | Quality gates configured and green: ruff (format + lint), mypy, pytest | `uv run ruff format --check . && uv run ruff check . && uv run mypy && uv run pytest tests -q` | each command exits `0`; pytest reports the unit, integration and e2e layers | REQ-NF-004, REQ-NF-008 |
| ACC-M0-010 | No secrets, certificates, environment files or real traffic captures are committed | `git grep -nE "BEGIN (RSA|EC|DSA|OPENSSH|PRIVATE) KEY" -- . ; test ! -e .env ; git status --porcelain` | no matches, no `.env` file, no unintended files in `git status` | REQ-NF-006 |
| ACC-M0-011 | CI workflow runs the gates in layers and verifies the lock file | `uv run python -c "import yaml,pathlib;print(sorted(yaml.safe_load(pathlib.Path('.github/workflows/ci.yml').read_text())['jobs']))"` | `['e2e', 'integration', 'lint', 'type-check', 'unit']` | REQ-NF-008 |
| ACC-M0-012 | Production gap register contains the baseline of `AGENT.md` section 3 | `grep -c '^| ' docs/production-gaps.md` | 13 or more table rows, one per baseline area (transport, authentication, topology, core network, header handling, transactions, reliability, media, charging, security, observability, configuration, capacity) | REQ-NF-007 |

## M1 — Signalling path (executed 2026-09-16)

| ID | Criterion | Verification command | Expected result | Requirement |
| --- | --- | --- | --- | --- |
| ACC-M1-001 | A complete call runs: `INVITE → 100 → 180 → 200 OK → ACK → BYE` | `uv run pytest tests/e2e -q -k complete_call` | the e2e test passes and prints a Call-ID keyed trace | REQ-F-002, REQ-F-009 |
| ACC-M1-002 | Headers and SDP pass through unmodified | `uv run pytest tests/integration -q -k pass_through` | the outbound INVITE carries the same **pass-through** headers and the same SDP body; the Request-URI differs, and so do the dialog identity (Call-ID `<trunk>-b2b_1`, regenerated `From` tag and `CSeq`) and `User-Agent` | REQ-F-008 |
| ACC-M1-003 | A request from an unlisted source is rejected with `403` and `AS-PEER-001` | `uv run pytest tests/integration -q -k peer` | the test passes | REQ-F-007 |
| ACC-M1-004 | Counters, health endpoint and graceful `SIGTERM` shutdown work | `uv run pytest tests/integration -q -k lifecycle` | the test passes; `/healthz` answers `{"status":"ok",...}`, `/api/v1/metrics` serves the counters, and the log ends with `shutdown complete` and `reason: signal SIGTERM` (exit code `0`) | REQ-F-011 |
| ACC-M1-005 | Message samples are captured from a real call, never hand-written | `uv run python tools/capture_call.py` | 14 files written to `docs/specs/message-samples/` following `NN-direction-method[-qualifier].txt`, one per message of `INVITE → 100 → 180 → 200 → ACK → BYE`; the **pass-through** headers and the SDP of `01-in-invite-trunk.txt` reappear unchanged in `03-out-invite-core.txt`, while the outbound Call-ID is the inbound one plus `-b2b_1`. The directory is generated and gitignored (only its `README.md` is tracked), so the evidence is reproduced with `make capture`, not read from the repository | REQ-NF-007 |
| ACC-M1-006 | The mock S-SBC runs as its own process and answers the AS on configurable ports | `docker compose -f deploy/docker-compose.yml config` and `uv run python -m s_sbc_mock.main --help` | the compose file validates and lists the `s-sbc-mock` service with UDP ports; the process entry point exposes `--listen-address`, `--listen-port`, `--trunk-port`, `--as-address`, `--as-port`, `--call` and `--repeat` | REQ-F-001, REQ-NF-009 |

## M2 — Number translation (executed 2026-09-16)

| ID | Criterion | Verification command | Expected result | Requirement |
| --- | --- | --- | --- | --- |
| ACC-M2-001 | The Request-URI and number format are rewritten per rules | `uv run pytest tests/e2e -q -k translation` | `+8613800138000` leaves the AS as `013800138000` (rule `R-MOB-CM-40`) | REQ-F-003 |
| ACC-M2-002 | Next hop failover is used when the first hop is unavailable | `uv run pytest tests/integration -q -k failover` | the second next hop receives the INVITE and the call completes with `200 OK` | REQ-F-004 |
| ACC-M2-003 | Error branches: `404`, `603`, `CANCEL` | `uv run pytest tests/e2e -q` | all error-branch tests pass (`404` / `AS-ROUTE-001`, `603` / `AS-ROUTE-002`, `CANCEL`); `480` / `500` covered at unit level | REQ-F-006 |
| ACC-M2-004 | YAML hot reload: a changed rules file activates at runtime; a broken one keeps the previous rule set | `uv run pytest tests/integration -q -k reload` | both reload tests pass (ADR-0004) | REQ-F-005 |
| ACC-M2-005 | Translated-call message samples captured, not hand-written | `uv run python tools/capture_call.py` | 14 files in `docs/specs/message-samples/`; `01-in-invite-trunk.txt` carries `+8613800138000`, `03-out-invite-core.txt` carries `013800138000`, and the outbound Call-ID is the trunk Call-ID plus the AS suffix `-b2b_1` (the second leg's own dialog identity). The directory is generated and gitignored (only its `README.md` is tracked); reproduce with `make capture` | REQ-NF-007 |

## M3 — Console (executed 2026-09-16)

| ID | Criterion | Verification command | Expected result | Requirement |
| --- | --- | --- | --- | --- |
| ACC-M3-001 | Console shows live flow, rule hit, statistics and topology, with no third-party front-end libraries | `uv run pytest tests/integration -q -k console` | the test passes; no external script or stylesheet reference in the page | REQ-F-012, REQ-NF-010 |
| ACC-M3-002 | Internal API serves health, metrics, rules and traces | `curl -s http://127.0.0.1:8080/healthz` | `{"status":"ok",...}` | REQ-F-011 |

## M4 — Acceptance and polish (executed 2026-09-16)

| ID | Criterion | Verification command | Expected result | Requirement |
| --- | --- | --- | --- | --- |
| ACC-M4-001 | Every acceptance item carries the four kinds of evidence | review of `docs/acceptance/report.md` | no item without evidence | REQ-NF-007 |
| ACC-M4-002 | `docs/demo-script.md` rehearsed end to end | `make demo` | the narrated flow completes | REQ-NF-008 |

## Phase 2 — P8a sippy retransmission-timer shutdown (executed 2026-09-18)

| ID | Criterion | Verification command | Expected result | Requirement |
| --- | --- | --- | --- | --- |
| ACC-P8A-001 | Stopping the signalling stack leaves no per-transaction timer armed, so a retransmission that was pending can never outlive its transaction manager | `uv run pytest tests/integration/test_signalling_path.py tests/unit/test_sip_adapter.py -q` | every test passes; `test_stopping_the_stack_leaves_no_transaction_timer_armed` first asserts that the abandoned first-hop INVITE really left a pending retransmission and then asserts that no `ED2` timer owned by the manager survives `AsStack.stop()`. Removing the cancellation makes it fail with `AssertionError: 2 timer(s) still armed on a stopped transaction manager` | REQ-F-011, REQ-NF-004 |

## Phase 2 — P8 anti-fraud AS (executed 2026-09-19)

| ID | Criterion | Verification command | Expected result | Requirement |
| --- | --- | --- | --- | --- |
| ACC-P8-001 | The anti-fraud AS runs as a **second, independently runnable process** (`python -m anti_fraud_as.main`): its own SIP listen port (default `5062`, not the first AS's `5060`), its own declarative screening file under `config/`, its own internal API/console feed, a startup self-check that aborts on an unusable configuration or a taken port, and a stop path that cancels every timer it armed | `uv run python -m anti_fraud_as.main --self-check-only; echo $?; uv run pytest tests/integration/test_fraud_screening_path.py tests/unit/test_fraud_configuration.py -q` | exit code `0` and the log lines `anti-fraud application server starting` / `startup self-check passed`; then `28 passed`. The integration file runs the AS as a real process (`test_the_process_serves_health_and_exits_zero_on_sigterm`: `/healthz` reports `instance: anti-fraud`, `SIGTERM` exits `0` with `shutdown complete` / `reason: signal SIGTERM`), shows the loop-owned reload in a live process, and asserts `test_stopping_the_process_leaves_no_timer_armed` (the controller's no-answer timer is cancelled and no timer owned by the stopped manager remains scheduled). `tests/unit/test_fraud_configuration.py` pins the loopback defaults, the `FRAUD_*` knobs declared in `.env.example`, the fail-fast self-check and that the package adds **no** third-party dependency (`test_the_fraud_package_adds_no_third_party_dependency`) | REQ-F-016, REQ-F-021, REQ-NF-014 |
| ACC-P8-002 | The verdict is taken on the **calling** party. An **allowed** INVITE is relayed as a B2BUA (`INVITE → 100 → 180 → 200 OK → ACK → BYE`) with the Request-URI and the SDP body kept, the pass-through header set copied and **no header added**; a **rejected** INVITE is answered on the trunk with the full line `SIP/2.0 608 Rejected`, carries **no `Call-Info`**, and originates **no second leg** (UAS behaviour, not B2BUA) | `uv run pytest tests/e2e/test_fraud_call_flows.py tests/integration/test_fraud_screening_path.py -q` | `15 passed`. The integration file asserts the **full final status line** `SIP/2.0 608 Rejected` in the recorded wire bytes of the rejected call, that the core side of the mock received **no INVITE** (the absence of the second leg is the assertion, with a positive control proving the core-side recorder sees the relayed allowed call), that the relayed INVITE adds no header (`set(relayed) <= set(sent) | {"content-length"}`, no `Call-Info`), and that a UAC that never declared `sip.608` is still answered `608` with no body. The e2e file drives both flows and prints a Call-ID keyed trace | REQ-F-017, REQ-F-019, REQ-F-021 |
| ACC-P8-003 | The verdict is computed from caller **reputation** (a score that decays over time), a per-caller **call-rate window** and **block/allow lists**, read from a declarative file under `config/` that is validated on load and reloaded fail-safe; the signal order is allow list → block list → rate window → reputation and the deciding signal is named. The mock UAC declares `Feature-Caps: *;+sip.608`; the AS plays **no media** and answers `608` whether or not the declaration is present | `uv run pytest tests/unit/test_screening_engine.py tests/unit/test_screening_data.py tests/integration/test_fraud_screening_path.py -q` | `58 passed`. The engine tests pin the signal order, the window/reputation thresholds and that the decision names its source; the data tests pin the shipped file, the derived `BL-`/`AL-` identifiers, schema validation and the fail-safe reload; the integration tests drive the window and then the reputation through the real controller with an injected clock, reject an undeclared-`sip.608` UAC with `608` (recording `reject.sip_608_undeclared`), and assert the `608` carries no `Call-Info` and no body | REQ-F-018, REQ-F-019, REQ-F-020, REQ-NF-013 |
| ACC-P8-004 | The verdict is a **pure function** (no sockets, no global state, no clock; time is injected), and cross-call state — the call-rate window and the reputation ledger — lives in a **process-level module**, never in the per-call controller; the state is **in memory**, so a restart loses it (a registered POC gap) | `uv run pytest tests/unit/test_caller_state.py tests/unit/test_screening_engine.py -q` | `37 passed`. `test_the_engine_reads_no_clock` (engine) and `test_the_store_reads_only_the_injected_clock` (store) assert clock ownership directly; `test_reputation_decays_halfway_back_after_one_half_life` and `..._after_two_half_lives` pin the decay; the store is bounded across callers and keeps history across a reconfigure. **No test covers the "a restart loses it" half** — it is a non-behaviour covered by the gap register, not an executed check | REQ-F-022, REQ-NF-011, REQ-NF-012 |
| ACC-P8-005 | New `AS-FRAUD-*` codes live in the one authoritative model in `src/as_app/errors.py` and map to SIP status codes and log messages; the verdict, its deciding signal/score and the matched list entry are observable through counters, the Call-ID keyed trace and the internal-API/console payloads (read-only) | `uv run pytest tests/unit/test_fraud_error_model.py -q` | `18 passed`. `test_sip_phrases_maps_608_to_rejected`, `test_the_screening_rejections_are_answered_with_608`, `test_the_configuration_failures_are_answered_with_500`, `test_fraud_codes_live_in_the_shared_error_model` and `test_the_fraud_codes_are_unique_and_prefixed` pin the codes; `test_metrics_payload_exposes_the_counters_next_to_the_errors`, `test_the_internal_api_covers_the_console_surface` and `test_the_screening_payload_is_read_only_json` pin observability. **The `AS-FRAUD-006` fallback is untested** (see the report's accepted limitations) | REQ-F-023, REQ-F-024 |
| ACC-P8-006 | The `608` reject path is verified **by running sippy over UDP**, not assumed: the committed design probe answers a real INVITE with `SIP/2.0 608 Rejected` through `CCEventFail((status, phrase, None))`. The probe is a design instrument, not a test, and does not run in CI; the same behaviour's on-wire guard is the integration assertion of the full status line (ACC-P8-002) | `uv run python tools/anti_fraud_probe.py; echo $?` | the `--- verdict ---` block shows `final status line: SIP/2.0 608 Rejected`, `expected : SIP/2.0 608 Rejected` and `CCEventFail 608 'Rejected' reject path: OK`, exit code `0`. The probe compares code **and** reason phrase and exits non-zero on any mismatch; it is not collected by pytest and is not part of the gate | REQ-NF-015 |

## Phase 2 — P9 chained topology (executed 2026-09-19, **superseded by P9b**)

> Trunk-to-trunk wiring (`FRAUD_SBC_PEER_* → AS-2`) is obsolete. Use the **P9b** rows below
> for current verification. These rows remain as historical evidence of the 2026-09-19 delivery.

| ID | Criterion | Verification command | Expected result | Requirement |
| --- | --- | --- | --- | --- |
| ACC-P9-001 | The chain `SBC → AS-1 (anti-fraud) → AS-2 (number translation) → core` runs end to end: an INVITE AS-1 **allows** is relayed into AS-2, translated there and answered by the core, driving a complete call (`INVITE → 100 → 180 → 200 OK → ACK → BYE`) through both B2BUA instances, and it is chained **by configuration only** — AS-1's `FRAUD_SBC_PEER_*` points at AS-2's listen address and AS-2's next hop is a routing-catalogue entry, with **no** iFC emulation in the mock. The two instances stay independent in **one** direction: the unit invariant asserts `src/as_app` must not import `anti_fraud_as`; the reverse (`anti_fraud_as/call_controller.py` importing `as_app.sip_adapter`) is by design (ADR-0007 decision 9) — **the P9-era mechanism has since changed**: after P10 the anti-fraud controller imports `as_platform.sip_adapter` **directly**, so the extraction replaced ADR-0007 decision 9's *mechanism* while its *substance* (a second process, not a framework) still holds, and the package's only remaining `as_app` **import** is the version chain in `src/anti_fraud_as/__init__.py` (ADR-0009 decision 2, HLD §10.3). The clause above stays as the P9 record and the one-way invariant it justifies is unchanged, so the criterion describes the one-way independence and does **not** claim "neither AS imports the other" | `uv run pytest tests/integration/test_chained_topology.py -q; uv run pytest tests/unit/test_repository_baseline.py -q -k import_the_anti_fraud` | `3 passed` (`test_an_allowed_call_traverses_both_b2bus_and_is_translated`, `test_every_leg_regenerates_its_call_id_and_each_instance_keys_its_trace`, `test_a_reject_at_as1_short_circuits_before_as2_and_the_core`): the core receives exactly one INVITE carrying `013800138000`, and the SDP body and the pass-through headers are byte-identical across both B2BUs; then `1 passed` (`test_as_app_does_not_import_the_anti_fraud_as`). **Note: the e2e test asserts only `INVITE`, `180`, `200` and `BYE` in each trace — `ACK` is not asserted by any P9 test**, because the mock's UAC never records `ACK` in a trace (REQ-F-025 lists `INVITE → 100 → 180 → 200 OK → ACK → BYE`; see the report's accepted limitations) | REQ-F-025, REQ-F-026 |
| ACC-P9-002 | A `608` reject at AS-1 **short-circuits** the chain: AS-2 creates no call state and the core receives no INVITE, the full line `SIP/2.0 608 Rejected` goes over the trunk, and no second leg is originated (UAS behaviour) | `uv run pytest tests/integration/test_chained_topology.py tests/e2e/test_chained_call_flows.py -q -k reject` | `2 passed, 3 deselected` (`test_a_reject_at_as1_short_circuits_before_as2_and_the_core`, `test_a_rejected_call_ends_with_608_at_as1_and_reaches_nothing_else`). Both assert the short-circuit as a **delta of zero** at AS-2 (`tracer.known_call_ids()`) and at the core (`mock.uas.received_invites`), not merely a status code | REQ-F-027 |
| ACC-P9-003 | The chained call is observable **per instance** — each AS writes its own Call-ID keyed trace, keyed on the value it saw on its own trunk leg — while cross-AS `Call-ID` correlation is **not** solved: the chain carries **three distinct** `Call-ID`s (trunk `X`, inter-AS `X-b2b_1`, core `X-b2b_1-b2b_1`) and each instance's trace holds events only for its own value | `uv run pytest tests/integration/test_chained_topology.py tests/e2e/test_chained_call_flows.py -q; uv run python tools/chained_as_probe.py; echo $?` | `5 passed`; then the probe prints `distinct Call-IDs: 3`, `Call-ID per leg: True`, `ICID preserved: True` and the `--- verdict ---` block reads `allowed call completed through two B2BUAs : OK`, `608 reject short-circuited before AS-2 : OK`, `Call-ID regenerated on every leg : OK`, `ICID preserved across every leg : OK`, exit code `0`. The integration test also asserts each instance's trace is **not** keyed on the other's Call-ID | REQ-F-028, REQ-NF-016 |
| ACC-P9-004 | The chained topology is demonstrated by a **first-class, documented run command** (`make demo-chained` → `tools/demo_chained_call.py`) runnable from a clean checkout, mirroring `make demo` / `make demo-fraud`. Chaining reuses the existing peer/listen knobs: the unit invariant asserts no declared `.env.example` key names a `chain` knob (the "no new port" half is not asserted by a test — see the report's accepted limitations) | `uv run pytest tests/unit/test_repository_baseline.py -q -k "demo_chained or chaining"; uv run python tools/demo_chained_call.py --rules-file config/routing_rules.yaml --screening-file config/caller_screening.yaml; echo $?` | `2 passed, 51 deselected` (`test_make_demo_chained_is_a_documented_first_class_entry_point` — the `Makefile` target plus `AGENT.md` / `README.md` / `docs/README.md` / `tools/README.md`; `test_chaining_added_no_new_configuration_knob`); then the demo transcript shows three `Call-ID`s, the three equal `ICID` rows and a `--- verdict ---` block of five `OK` lines, exit code `0` | REQ-NF-017 |
| ACC-P9-005 | *(historical — superseded by P9b)* The friction the chained demo surfaced — what in the shared skeleton turned out to be number-translation specific — is **recorded** as P10's primary input: the P9 section of `docs/production-gaps.md` carries the five rows (iFC/ISC emulation, cross-AS trace correlation, no shared state, routing-catalogue coupling, chain failure/ordering/capacity semantics). Verified by the register, **not** by a test | `grep -n -e 'iFC / ISC emulation' -e 'Cross-AS trace correlation' -e 'Shared state between the two instances' -e 'Routing catalogue coupling' -e 'Chain failure, ordering and capacity semantics' docs/production-gaps.md` | five matches, all under `## Additional gaps registered while building P9 (chained AS topology, 2026-09-19)`, exit code `0` | REQ-NF-018 |

## Phase 2 — P9b chained topology rework (executed 2026-09-23)

| ID | Criterion | Verification command | Expected result | Requirement |
| --- | --- | --- | --- | --- |
| ACC-P9b-001 | Allow path via iFC orchestration (`ims_mock`); peer knobs are S-SBC return only | `NO_PROXY=127.0.0.1,localhost uv run pytest tests/integration/test_chained_topology.py::test_an_allowed_call_traverses_both_b2bus_and_is_translated -q` | `1 passed` | REQ-F-025, REQ-F-026 |
| ACC-P9b-002 | Each AS receives its own trunk INVITE (iFC #1 and #2) | `NO_PROXY=127.0.0.1,localhost uv run pytest tests/integration/test_chained_topology.py -q` | `3 passed` | REQ-F-025 |
| ACC-P9b-003 | Four distinct AS-leg Call-IDs; ICID preserved | `NO_PROXY=127.0.0.1,localhost uv run python tools/chained_as_probe.py; echo $?` | `distinct Call-IDs : 4`, exit `0` | REQ-F-028, REQ-NF-016 |
| ACC-P9b-004 | 608 at AS-1 short-circuits before AS-2 and terminating UAS | `NO_PROXY=127.0.0.1,localhost uv run pytest tests/integration/test_chained_topology.py tests/e2e/test_chained_call_flows.py -q -k reject` | `2 passed` | REQ-F-027 |
| ACC-P9b-005 | `make demo-chained` passes | `NO_PROXY=127.0.0.1,localhost make demo-chained; echo $?` | five `OK` lines, exit `0` | REQ-NF-017 |
| ACC-P9b-006 | `make demo` and `make demo-fraud` regression | `NO_PROXY=127.0.0.1,localhost make demo; make demo-fraud` | both exit `0` | REQ-F-025 |
| ACC-P9b-007 | Full dialog through both AS instances | `NO_PROXY=127.0.0.1,localhost uv run pytest tests/e2e/test_chained_call_flows.py::test_the_complete_chained_call_runs_invite_to_bye -q` | `1 passed` | REQ-F-025 |
| ACC-P9b-008 | Ten concurrent chained calls | `NO_PROXY=127.0.0.1,localhost uv run pytest tests/integration/test_concurrent_load.py -q -k chained` | `2 passed` | REQ-F-028 |

## Phase 2 — P10 platform extraction (executed 2026-09-20)

| ID | Criterion | Verification command | Expected result | Requirement |
| --- | --- | --- | --- | --- |
| ACC-P10-001 | The skeleton shared by the two AS instances is **extracted into a library in a new repository**, checked out beside this one at `../as_platform` (branch `main`, distribution `as-platform`, import package `as_platform`, its own `VERSION` and its own standard documents), and both instances — `src/as_app/` (number translation) and `src/anti_fraud_as/` (anti-fraud) — are **users** of that library instead of owners of the shared code. The library is a real, resolvable repository, not a copy of this repository's source tree | `git -C ../as_platform log --oneline; cat ../as_platform/VERSION; uv sync --frozen; uv run pytest tests/unit/test_repository_baseline.py -q -k users_of_the_platform_library` | the library's log ends at `4caec3e chore: create the as-platform library repository skeleton` and carries the extraction commits (`9eebe66` … `0eeef37`); `VERSION` is `0.1.0`; `uv sync --frozen` succeeds (`Checked 45 packages …`); then `1 passed, 55 deselected` (`test_both_as_instances_are_users_of_the_platform_library`). **The second half of the requirement — "this repository becomes the library's reference implementation" — is a role statement, not a property of the artefacts, so no test asserts it** (see the report's accepted limitations) | REQ-F-029 |
| ACC-P10-002 | The library is **independent of both use cases**: no module under `../as_platform/src/` imports `as_app` or `anti_fraud_as`, so it carries the skeleton and not either use case. The one-way invariant this repository already held — `src/as_app/**` must not import `anti_fraud_as` — survives the extraction unchanged | `grep -rnE 'as_app\|anti_fraud_as' ../as_platform/src/; echo $?; uv run pytest ../as_platform/tests/test_library_independence.py -q; uv run pytest tests/unit/test_repository_baseline.py -q -k import_the_anti_fraud` | the grep prints **no line** and exits `1` (no match); then `2 passed`; then `1 passed, 55 deselected` (`test_as_app_does_not_import_the_anti_fraud_as`). The reverse direction is **by design**: `anti_fraud_as` may import library modules, and since the extraction `src/anti_fraud_as/call_controller.py:57` imports `as_platform.sip_adapter` **directly** — the extraction replaced ADR-0007 decision 9's *mechanism* (a direct import from `as_app`) while that decision's *substance* (a second process, not a framework) still holds — and the package's only remaining `as_app` **import** is the version chain, `src/anti_fraud_as/__init__.py:31` `from as_app import __version__` (ADR-0009 decision 2, HLD §10.3). So the criterion claims only the library's independence and the one forbidden application direction | REQ-F-030 |
| ACC-P10-003 | The two AS instances stay **independent processes** and their externally observable behaviour is **unchanged** by the extraction: the same SIP signalling on the trunk, the same `AS-*` error codes and the same per-instance Call-ID keyed trace. The extraction is a pure refactor with no wire-visible change, so the three existing layers stay green under the same gate | `make lint; make unit; make integration; make e2e` | `96 files already formatted`, `All checks passed!`, `Success: no issues found in 29 source files`; then `210 passed`, `36 passed`, `9 passed`. The counts are **above** the pre-extraction baseline (`255` total against `246`) because the implementation and tests stages **added** tests (the trunk-answer regression, the fraud-controller overrides, the sibling-checkout and workspace rows); **no test was deleted, loosened or reclassified**, and the wire behaviour the layers assert is the same | REQ-F-031 |
| ACC-P10-004 | This repository consumes the library through a **`path` dependency in `pyproject.toml`** — `../as_platform`, `editable = true` — so the `AGENT.md` §10 guarantee *"clone → `uv sync` → `make demo`"* becomes *"clone **both** repositories side by side"*. That is a known and accepted cost, recorded as an explicit exception rather than silently weakening the guarantee, and the `uv` behaviour behind the mechanism is **measured, not assumed** | `uv run pytest tests/unit/test_repository_baseline.py -q -k "sibling_checkout or workspace"; uv run python tools/path_dependency_probe.py; echo $?` | `2 passed, 54 deselected` (`test_the_library_is_consumed_from_the_sibling_checkout` — `[project].dependencies`, `[tool.uv.sources]` `path`, `editable = true`; `test_the_library_is_not_a_uv_workspace_member`); then the probe prints `cases measured : 8`, `expectations : all held` and exit code `0`, including `(b) default install is a copy; --reinstall-package refreshes it : OK`, `(b) editable = true links the checkout; the edit is visible at once : OK`, `(c) a version constraint is silently ignored : OK` and `(f) a nested consumer does not find a sibling library : OK`. **The probe measures `uv` against a two-module stand-in, not the real skeleton** (`docs/production-gaps.md`, row *Consumption probe's stand-in scope*), and **the single-checkout failure cannot be a test here**: this working tree *has* the sibling, so "a clone without it fails to resolve" is recorded as the accepted cost, not asserted | REQ-F-032 |
| ACC-P10-005 | The library is a **library, not a running service**: it follows the library documentation standard (an **API reference**, an **integration guide** and a **compatibility matrix**) and does **not** copy this repository's application document set, and it is **not a uv workspace monorepo** — its manifest self-reports `as-platform` and declares no `[tool.uv.workspace]` table, and neither does this repository's | `ls ../as_platform/docs/; uv run pytest ../as_platform/tests/test_library_standard.py -q; uv run pytest tests/unit/test_repository_baseline.py -q -k workspace` | exactly `api-reference.md`, `compatibility-matrix.md`, `integration-guide.md`; then `6 passed`, including `test_the_library_is_a_standalone_distribution_not_a_workspace_member` (no `[tool.uv.workspace]` table **and** `[project]` self-reports `as-platform`) and the three-document and "application set not copied" checks; then `1 passed, 55 deselected` for this repository's half. Nothing under `../as_platform/docs/` is one of this repository's application documents | REQ-NF-019 |
| ACC-P10-006 | The two pluggable dimensions are **enabled by, but not built in, P10**: the library defines the `Transport` and `StateStore` seams and ships exactly **one** implementation of each (`UdpTransport`, `InMemoryStateStore`), and P11's second transport (TLS), external store (Redis) and capacity harness are **absent**. The guard is a vocabulary check over the library's **module names** — not module content or class names — so a file named for any of the three fails the library's own suite | `uv run pytest ../as_platform/tests/test_seams.py -q` | `4 passed`, including `test_the_library_ships_no_tls_no_redis_and_no_capacity_harness` (a lower-cased **substring** match over the module stem: `tlsconfig.py` and `redis_store.py` both turn it red) and `test_the_library_defines_no_second_transport_and_no_second_state_store` (the `*Transport` / `*StateStore` class-suffix check). The assertions are a **deliberate tripwire that P11 rewrites by design** when it adds TLS and Redis behind the seams | REQ-NF-020 |
| ACC-P10-007 | The extraction is **staged, not all-or-nothing** (ADR-0009 decision 7's seven steps): the skeleton moves into the library in a sequence of commits on two repositories, and each step leaves this repository building, linting and passing its three layers — a step that would leave it broken is not a valid step. The **sequence** is what the criterion verifies; the per-step green is a historical property, re-run here only at two spot checks | `git log --oneline f041174~1..0b12db2; git worktree add /tmp/p10-step1 f041174` (with the library checked out at `4caec3e` beside it) then the four gates; likewise for step 4 (`0319c8d` + library `060ef71`) | the log lists the sequence in order: `f041174` build (step 1) → `ac929a9`, `efa4386` (steps 2–3) → `0319c8d` (step 4) → `f341aff`, `8ab507d` (step 5) → `0b12db2` (step 6), with the library's `main` landing the same moves (`9eebe66`, `ee02653`, `7c1355b`, `060ef71`, `f82a819`, `734d82c`, `d5e4561`, `d627e73`, `aaa453e`). The two spot checks ran the four gates green — step 1 `94 files already formatted` / `246 passed`, step 4 `95 files already formatted` / `246 passed` — and both temporary worktrees were removed afterwards. **No test asserts this**: it is a process and historical property, not a property of the artefacts (see the report's accepted limitations) | REQ-F-033 |
| ACC-P10-008 | The new repository carries **its own test suite and its own gate** — the same four commands as this repository (`ruff format --check`, `ruff check`, `mypy`, `pytest`) — so a change to the library is verifiable **where the library lives**. The suite covers the pure helpers and the sippy adapter boundary plus the library-level independence assertion of `REQ-F-030`; the `AGENT.md` §11 three layers are the **application's** and do not transfer to a library | `make lint; uv run pytest -q` (run in `../as_platform`); `uv run python -c "import yaml,pathlib;print(sorted(yaml.safe_load(pathlib.Path('../as_platform/.github/workflows/ci.yml').read_text())['jobs']))"` | `33 files already formatted`, `All checks passed!`, `Success: no issues found in 15 source files`, then `84 passed`; and the workflow declares `['lint', 'test', 'type']` (three jobs, each `uv sync --frozen`, triggered on `push` / `pull_request` to `main`). **Kind 3 (a CI result) is not producible**: the library has **no remote and has never been pushed**, so its workflow is an unexecuted definition and no CI run exists for these commits — see the report's §3, and `docs/production-gaps.md` rows *CI second checkout (new dependency)* and *Library gate not in this repository's CI* | REQ-NF-021 |

### Phase 2 — P11 platform verification

| ACC-P11-001 | The Transport seam ships **two implementations**: `UdpTransport` (default) and `TlsTransport` (TLS terminator). `TlsTransport` bridges encrypted TLS socket ↔ decrypted sippy-local UDP without modifying sippy — it uses the same `sip_config()` mechanism the seam defined in P10, and Protocol lifecycle methods (`start`/`stop`) manage its sockets and threads; `UdpTransport` supplies no-op methods because sippy manages the UDP server itself | `uv run pytest ../as_platform/tests/test_seams.py -q -k transport`; `uv run pytest ../as_platform/tests/test_transport.py -q` | `2 passed` for the seam check (`test_the_transport_seam_exports_the_boundary_and_two_implementations`, `test_the_library_defines_two_transports_and_two_state_stores` transport half); then `9 passed` including `test_tls_transport_start_allocates_a_local_udp_port`, `test_tls_transport_start_then_stop_releases_resources`, `test_tls_transport_sip_config_before_start_raises`. The certificate-generation step for TLS tests uses `openssl req -x509 ...` at runtime (no cert committed) — ADR-0010 decision 1's sippy-external bridge is what every test drives | REQ-F-034, REQ-NF-022 |
| ACC-P11-002 | The StateStore seam ships **two implementations**: `InMemoryStateStore` (default, no change from P10) and `RedisStateStore` (second store, background-worker queue pattern). `RedisStateStore` preserves the `StateStore` protocol's synchronous `read`/`write`/`trim` signatures but performs Redis I/O on a dedicated worker thread, so sippy callbacks never block (AGENT.md §6). The `trim` method is best-effort — Redis has no native per-prefix LRU eviction — but reproduces the in-memory store's *purpose* (drop oldest of a family), not its exact mechanism | `uv run pytest ../as_platform/tests/test_seams.py -q -k state_store`; `uv run pytest ../as_platform/tests/test_state_store.py -q` | `2 passed` for the seam check; then `10 passed, 5 skipped` — the 5 skipped are the `RedisStateStore` tests that require a running `redis://localhost:6379/15` and are gated by `pytest.mark.skipif(not _redis_available())`. **None of the 5 skipped is a library-level failure** — the guard correctly identifies the absence of Redis on this machine, exactly as designed. The non-skipped `InMemoryStateStore` tests (including `test_in_memory_store_start_and_stop_are_no_ops`) are green. Background-worker correctness is asserted by `test_store_works_from_multiple_threads` (a Redis test, skipped here); the pattern is verified structurally in `state_store.py`'s `_run()` worker | REQ-F-035, REQ-NF-023 |
| ACC-P11-003 | `NextHop.transport` is **widened** from `Literal["udp"]` to `Literal["udp", "tls"]` so a B2BUA next hop can declare TLS as its transport. The extension is purely additive: every existing `"udp"` value keeps working unchanged, and P10's consumer repositories require no code changes to consume the widened field | `uv run pytest ../as_platform/tests/test_hop.py -q`; `uv run pytest -q tests/integration/test_translation.py -k failover 2>&1 | tail -3` | `3 passed` for the hop tests (`test_next_hop_default_transport_is_udp`, `test_next_hop_accepts_tls_transport`, `test_next_hop_rejects_unknown_transport`); then `1 passed` for the failover test, proving the widened field did not break any P10 consumer | REQ-F-036 |
| ACC-P11-004 | The capacity harness is a **first-class library module** (`as_platform.capacity_harness`) that drives a call factory at configurable concurrency levels and records per-level observations (completed/timed_out counts and loop-gap measurement). It is **not** an application — it has no sockets, no sippy loop and no mock — and it **does not publish benchmark numbers** (D10, REQ-NF-025). Every caller observation goes to the caller's callback, trace or counters — never to README or CHANGELOG as a performance claim | `uv run pytest ../as_platform/tests/test_capacity_harness.py -q`; `grep -c 'benchmark\|calls-per-second\|throughput' ../as_platform/README.md ../as_platform/CHANGELOG.md ../as_platform/docs/*.md | tail -10` | `7 passed` including `test_driver_runs_each_requested_level`, `test_driver_times_out_slow_calls`, `test_harness_uses_barrier_to_start_all_workers_at_once`; the grep command prints `0` for every file — no benchmark claim anywhere in the library's public documents | REQ-F-037, REQ-NF-025 |
| ACC-P11-005 | `UdpTransport` and `InMemoryStateStore` **remain the defaults** and require **no wiring change** in the application repositories. P10 and earlier consumer code continues to instantiate them without modification — the Protocol lifecycle methods (`start`/`stop`) that P11 added are **no-op** on the existing implementations, and `make demo` / the three application test layers stay green against the updated library | `make check` (as_platform); `uv run pytest -q tests/unit/ tests/integration/` (POC) | `99 passed, 5 skipped` on the library; `210 passed` on this repository's unit layer; then `154 passed` across the integration layer. The **only code change** required on the application side was zero — no import rewiring, no constructor change, no configuration new — the `hasattr`-free Protocol methods are satisfied by `start`/`stop` no-ops on `UdpTransport` and `InMemoryStateStore` | REQ-NF-024 |
| ACC-P11-006 | sippy's native lack of TLS/TCP is **recorded as a verified fact** (not assumed) and the bridge approach is what `TlsTransport` implements — the gap row "Transport" in `docs/production-gaps.md` is partially closed and the ADR records that `TlsTransport` terminates TLS outside sippy | `grep -A5 'sippy 2.4.2' docs/architecture/adr/0010-platform-verification-tls-redis-harness.md | head -20`; `grep 'Transport' docs/production-gaps.md | head -5` | The ADR carries "sippy has no SIP TLS support" / "sippy has no TCP transport either" / "Wss_server does use TLS — for WebSocket only" / "sippy's transport init is hard-wired to UDP" under its "Verified facts" heading, each quoting `SipTransactionManager.py` line numbers; the gaps row carries "UDP only" → "UDP + TLS; TLS mandatory on public-internet trunks" and P11 closes the first half (UDP + TLS now exists), leaving "TLS mandatory" as open | REQ-NF-022, REQ-NF-026 |

## Phase 3 — P12 Call Load capability (executed 2026-09-22)

| ID | Criterion | Verification command | Expected result | Requirement |
| --- | --- | --- | --- | --- |
| ACC-P12-001 | Load generator runs as a standalone tool (`tools/call_load_generator.py`), outside the AS processes, with a configurable concurrency pool (1–50), closed-loop leaky-bucket tick (~500 ms fill) and interactive REST control (start/stop/configure without restart) | `uv run python tools/call_load_generator.py --help; echo $?; uv run pytest tests/unit/test_call_pool.py -q; uv run pytest tests/unit/test_call_load_models.py -q` | help prints argparse groups for `--as-address`, `--target-concurrency` (default 10, range 1–50), `--call-rate` (default 3.0); `0` exit; `13 passed` for pool lifecycle + binding constraint + rate throttle; `18 passed` for models (CallModel, DurationModel, PoolConfig weights) | REQ-F-038 |
| ACC-P12-002 | Concurrency pool + call-rate throttle couple via Little's Law (`L = λW`): when rate×avg_duration > target, concurrency is binding; when rate×avg_duration < target, rate is binding. Binding constraint is exposed in status endpoint | `uv run pytest tests/unit/test_call_pool.py -q -k "binding"; uv run pytest tests/unit/test_generator_api.py -q -k status` | `test_binding_concurrency` and `test_binding_rate` and `test_binding_at_boundary` all pass (3/3 binding tests); the pool status snapshot carries which constraint is binding | REQ-F-039 |
| ACC-P12-003 | Generator points at either AS singly (translation or anti-fraud) **or** chained topology, selected by configuration. All 10 call types (T1–T6, F1–F4) are weighted-random selectable with enable/disable toggle | `uv run pytest tests/unit/test_call_load_models.py -q; uv run pytest tests/unit/test_call_pool.py -q -k "weighted"` | `18 passed` including weighted selection for call types and duration classes; `tests/unit/test_call_load_models.py` covers all 10 types (T1–T6 + F1–F4) and duration weights (D1 30%, D2 50%, D3 15%, D4 5%) | REQ-F-040 |
| ACC-P12-004 | Generator controls per-call simulated behaviour via duration classes: D1 fast (30%, BYE at 2s), D2 medium (50%, BYE at MIDPOINT 8–15s), D3 long (15%, BYE at 20–30s), D4 timeout (5%, no answer → AS 3 s no-answer tear-down) | `uv run pytest tests/unit/test_call_load_models.py -q; uv run python -c "from tools.call_load_generator import DurationModel; print(DurationModel.weighted_sample())"` | Weight table is [0.30, 0.50, 0.15, 0.05] for [Fast, Medium, Long, Timeout]; MockSipUac implements `CCEventConnect → Timeout(_send_bye, MIDPOINT)`, D4 → CANCEL at MIDPOINT=3s | REQ-F-041 |
| ACC-P12-005 | Both AS instances emit per-call event stream on internal API WebSocket: `call_started`, `call_routed`/`call_allowed`, `call_rejected`/`call_rejected_608`, `call_ended` — each keyed by Call-ID, enriched with source AS. `InternalApiServer` subclass eagerly builds FastAPI app with `SimplePublisher` fanout at `/ws/p12/events` | `uv run pytest tests/unit/test_internal_api.py -k "publisher or eager or route" -v; uv run pytest tests/integration/test_concurrent_load.py -k fanout -v` | 5 SimplePublisher tests (connect/broadcast/dead-prune/disconnect/empty), 2 eager-build tests (app exists before start, WS route mounted), 1 integration test (fanout plumbing reachable after `start_internal_api()`, `app.state.broadcast/publisher/_loop` all present). AS daemon thread captures its own `asyncio.new_event_loop()` in `app.state._loop` so `_emit_p12` uses `run_coroutine_threadsafe` — no silent event drop | REQ-F-042 |
| ACC-P12-006 | Concurrent call isolation: N calls (≥10) each complete independently — one call's BYE does not terminate another's dialog. P8a timer cleanup does not cross-contaminate: 10 concurrent calls with unreachable primary hops each independently time out and fail over, **not** collapse into one failover attempt | `uv run pytest tests/integration/test_concurrent_load.py -v` | 7 integration tests all pass: (1) 10 concurrent translation AS, all 200 OK; (2) 10 calls each get distinct outbound Call-IDs (no B2BUA leg collision); (3) fanout plumbing reachable; (4) 10 concurrent anti-fraud AS all 200 OK; (5) 10 concurrent chained topology all 200 OK + AS-1/AS-2 trace Call-IDs disjoint; (6) 10 concurrent failover (primary unbound) → each independently tries primary → failover → 200 OK → **core receives exactly 10 INVITEs** (proof of per-call timer independence). All genuine concurrent: every `place_call()` runs back-to-back without yielding — all 10 INVITEs sent before sippy event dispatch | REQ-F-043 |
| ACC-P12-007 | Generator exposes REST control surface: `POST /load/start`, `POST /load/stop`, `PUT /load/config` (`target_concurrency`, `call_rate`, `enabled_call_types`), `GET /load/status`. Also WebSocket feed for `pool_status_update` events on `/ws/load` | `uv run pytest tests/unit/test_generator_api.py -v` | 6 unit tests: pool starts, pool stops cleanly, config change takes effect at next tick, status reports binding constraint, REST endpoints respond, error cases (invalid config → HTTP 400) | REQ-F-044 |
| ACC-P12-008 | P12 does **not** modify `as_platform` library (`REQ-NF-027`). No new Transport, StateStore, error-code family or BaseAsStack method moves into `../as_platform`. Event emission hooks are AS-local `BaseCallController` overrides | `git diff --name-only main..HEAD -- src/as_platform/; echo "---"; git log --oneline main..HEAD | grep -i "platform" | head -5`; `ls ../as_platform/VERSION && cat ../as_platform/VERSION` | **Empty diff** in `src/as_platform/` (the library lives in a sibling repo, not inside this one); the commit log carries no `as_platform` moves — all AS-local changes (`src/as_app/call_controller.py`, `src/anti_fraud_as/call_controller.py`, `src/as_app/internal_api.py`, `src/anti_fraud_as/internal_api.py`, both `main.py`) are `_emit_p12` helper + `apply_call_policy`/`_record_disposition` overrides. `../as_platform/VERSION` stays `0.2.0` (P11's version) — P12 does not bump the library | REQ-NF-027 |
| ACC-P12-009 | Each AS instance (translation and anti-fraud) is its own process with its own sippy `ED2` event loop. P12 proves **within-AS** concurrent isolation — a property Phase 1/2 never tested because every demo and test ran one call at a time | `uv run pytest tests/integration/test_concurrent_load.py -v`; `uv run pytest tests/integration/test_signalling_path.py -q` | 10 concurrent translation calls each independently complete; 10 concurrent anti-fraud calls each independently complete; 10 concurrent chained calls each independently complete. The translation AS is one `AsStack` process (one ED2 loop), and within that loop 10 concurrent `CallController` instances each manage their own lifecycle | REQ-NF-028 |
| ACC-P12-010 | Generator is an external tool — it does **not** import `src/as_app`, `src/anti_fraud_as` or `as_platform`. It talks via SIP (mock S-CSCF UAC → AS INVITEs) and observes via event streams | `grep -rn "import.*as_app\|import.*anti_fraud_as\|import as_platform\|from as_app\|from anti_fraud_as\|from as_platform" tools/call_load_generator.py`; echo $? | **Exit code 1** — zero matches. Generator imports only stdlib (`argparse`, `asyncio`, `time`, `socket`, `dataclasses`, `random`) + third-party (`sippy`, `FastAPI`, `uvicorn`) + internal (`s_sbc_mock.uac.CallScenario`) — no AS source imports | REQ-NF-029 |
| ACC-P12-011 | P12 integration/e2e tests use **genuine concurrent load** — ≥10 concurrent calls, NOT sequential one-call-at-a-time. All 7 P12 integration tests place calls back-to-back without yielding to the sippy event loop | `grep -A3 "_place_n\|place_call.*for.*scenario" tests/integration/test_concurrent_load.py | head -20` | The helper `_place_n(trunk_pair, scenarios)` loops `place_call()` over all scenarios back-to-back — no `await asyncio.sleep`, no `run_until` between calls, no sequential waiting. All 10 INVITEs are sent before any sippy `CCEvent*` fires, so the tests exercise genuine concurrent interleaving | REQ-NF-030 |

## Phase 3 — P13 Enhanced Console (executed 2026-09-22)

| ID | Criterion | Verification command | Expected result | Requirement |
| --- | --- | --- | --- | --- |
| ACC-P13-001 | The console shows a **rolling live call-count line chart** (30-second window, ~500 ms ticks) implemented with vendored Chart.js. The canvas element `lineChart` exists on the page; the chart is initialised from `initCharts()` on page load; data points are pushed every 500 ms via `setInterval` from the AS event stream and `pool_status_update` events | `uv run pytest tests/integration/test_console.py -v -k "operations_ui or chartjs_canvases"` | `test_console_page_has_chartjs_canvases` passes (lineChart, pieChart, gaugeChart all present); `test_console_page_contains_operations_ui_elements` passes (Dashboard layout with line chart + gauge + pie + topology) | REQ-F-045 |
| ACC-P13-002 | The console shows a **call-state distribution pie/doughnut chart** driven by per-call AS events (`call_started`, `call_routed`/`call_allowed`, `call_rejected`/`call_rejected_608`, `call_ended`). The `pieChart` canvas exists; the state tracking variables and update function are present in the page JS | `uv run pytest tests/integration/test_console.py -v -k chartjs_canvases` | `test_console_page_has_chartjs_canvases` passes; `pieChart` canvas found in page | REQ-F-046 |
| ACC-P13-003 | The console shows a **capacity gauge** (active / target concurrency) as a semi-circular doughnut chart driven by `pool_status_update` events. The `gaugeChart` canvas exists; the gauge value element `gaugeVal` is present | `uv run pytest tests/integration/test_console.py -v -k chartjs_canvases` | `test_console_page_has_chartjs_canvases` passes; `gaugeChart` canvas found in page | REQ-F-047 |
| ACC-P13-004 | The console shows a **dynamic SVG topology** with 4 nodes: S-CSCF → Anti-fraud AS → Translation AS → core. Three link lines (l1/l2/l3) connect the nodes; link thickness scales with active call count; link colour changes with dominant state. The topology SVG is rendered inline (not an external image) | `uv run pytest tests/integration/test_console.py -v -k "operations_ui or static_scripts"` | Page contains `<svg` with 4 node labels: `S-CSCF`, `Anti-fraud`, `Translation`, `core`; 3 link lines with ids `l1`, `l2`, `l3` | REQ-F-048 |
| ACC-P13-005 | The console exposes **load generator controls**: target-concurrency slider (1–50), call-rate slider (0.1–10), Start/Stop buttons, and 10 call-type toggles (T1–T6, F1–F4). Controls call the generator REST API (`POST /load/start|stop`, `PUT /load/config`) and update from `pool_status_update` events | `uv run pytest tests/integration/test_console.py -v -k operations_ui` | Page contains `tgtSlider` (range 1–50), `rateSlider` (range 0.1–10), `btnStart`, `btnStop`, `typeToggles` container; `buildToggles()` function creates all 10 toggles | REQ-F-049 |
| ACC-P13-006 | Chart.js 4.x UMD bundle is **vendored locally** under `src/console/static/` — no CDN, no npm, no build step. The bundle (`chart.umd.min.js`) and its license (`chart.umd.min.js.LICENSE.txt`) are committed to the repository. All `<script src>` on the console page point to `/static/`. The bundle serves correctly from the console process | `uv run pytest tests/integration/test_console.py -v -k "vendored or separate_process"` | `test_console_page_has_only_vendored_static_scripts` passes (all scripts under `/static/`); `test_console_runs_as_separate_process_with_no_external_refs` passes (vendored bundle serves correctly from running console process, >10 KB, Chart.js content recognised) | REQ-F-050 |
| ACC-P13-007 | P13 does **not** modify AS source code or `as_platform` library — the enhanced console only *consumes* the P12 event stream and REST API. No changes to `src/as_app/`, `src/anti_fraud_as/`, or `../as_platform/` | `git diff --name-only main..HEAD -- src/as_app/ src/anti_fraud_as/ 2>/dev/null | grep -v internal_api || true; echo "---"; ls ../as_platform/VERSION && cat ../as_platform/VERSION` | P13-only changes are confined to `src/console/`, `tests/integration/test_console.py`, docs, and `AGENT.md`; `../as_platform/VERSION` stays `0.2.0` | REQ-NF-027 |
| ACC-P13-008 | The console preserves all legacy M3/P8 views (Call Trace, Rules, Screening, Statistics, About) accessible via the left navigation. Dashboard is the new default view but all existing panels are retained | `uv run pytest tests/integration/test_console.py -v -k "screening or operations_ui"` | Navigation entries for all 6 views (Dashboard, Call Trace, Rules, Screening, Statistics, About) are present; `vw-screening`, `vw-statistics`, `vw-rules`, `vw-call-trace`, `vw-about` containers exist | REQ-F-012 |
| ACC-P13-009 | Full test suite passes (307+ tests, 0 failures, 0 errors) and ruff lint is clean after P13 changes | `uv run pytest -q && uv run ruff check .` | 307 passed (was 306 before P13 +1 console test); `All checks passed!` from ruff | REQ-NF-004 |

## Phase 3 — P14 Phase 3 × P9b alignment (executed 2026-09-23)

| ID | Criterion | Verification command | Evidence | Requirement |
| --- | --- | --- | --- | --- |
| ACC-P14-001 | `scripts/phase3-demo.sh full` wires P9b iFC chain: `ims_mock.external_runtime`, both AS peer to S-SBC return | `grep -E 'external_runtime|IMS_RETURN|topology chained' scripts/phase3-demo.sh` | `full` mode starts `python -m ims_mock.external_runtime`; `FRAUD_SBC_PEER_PORT` and `SBC_PEER_PORT` use `$IMS_RETURN`; generator `--topology chained` | REQ-F-051 |
| ACC-P14-002 | Generator exposes `topology` and `ingress_port` on status; validates types per topology | `uv run pytest tests/unit/test_generator_api.py tests/unit/test_call_load_models.py -q -k 'topology or status or types_for'` | `topology`/`ingress_port` in status keys; HTTP 400 when F* enabled in `simple` topology | REQ-F-052 |
| ACC-P14-003 | Console shows mode badge and chained SVG (`topoMode`, `topoChained`) | `uv run pytest tests/integration/test_console.py -q -k operations_ui` | Page contains `topoMode`, `topoChained`, `setTopologyMode` | REQ-F-053 |
| ACC-P14-004 | Console dual WS + fraud health when `--fraud-api-url` set | `grep -E 'W_EV_F|fraud-api-url|connEvF' src/console/main.py` | `__FRAUD_API_URL__` token; `connEvF()`; `--fraud-api-url` CLI flag | REQ-F-054 |
| ACC-P14-005 | Topology-aware call-type toggles (client + server validation) | `uv run pytest tests/unit/test_generator_api.py -q -k 'f_types_in_simple'` | `applyToggleGating()` in console; `test_put_config_rejects_f_types_in_simple_topology` passes | REQ-F-055 |
| ACC-P14-006 | P14 does not modify `../as_platform` | `test -f ../as_platform/VERSION && cat ../as_platform/VERSION` | Library version unchanged at consumer discretion | REQ-NF-027 |
| ACC-P14-007 | `make demo` unchanged; generator remains external | `grep -L call_load_generator Makefile` | `make demo` target does not launch generator | REQ-NF-029 |
| ACC-P14-008 | Unit + integration suite green (307 tests) | `NO_PROXY=127.0.0.1,localhost uv run pytest tests/unit tests/integration -q` | 307 passed | REQ-NF-004 |
