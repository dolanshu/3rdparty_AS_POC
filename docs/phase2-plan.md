# Phase 2 plan — pointer (stub)

**This file is a stub. The canonical Phase 2 plan lives on the `phase2` branch.**

Phase 2 is what follows P1–P7: a second AS use case (anti-fraud, `608 Rejected`), a chained
two-AS demo, a read-only capacity probe, and the extraction of the shared skeleton into a
generic AS platform — items **P8–P11**.

The full text — strategic decisions and their rationale, the work sequence, the repository
and branch strategy, and the handover protocol — is `docs/phase2-plan.md` **on `phase2`**:

```
git checkout phase2                      # then read docs/phase2-plan.md
git show phase2:docs/phase2-plan.md      # read it without switching branch
```

**Edit the plan on `phase2`, not here.** One full text exists at any time; a second copy here
would drift. This stub is replaced by the full plan when `phase2` is merged into `main` at the
end of Phase 2, with the maintainer's approval per `AGENT.md` §13.

Pointers: `docs/roadmap.md` (section *Phase 2 — P8a … P11*) · `docs/README.md` · `AGENT.md` §15.
